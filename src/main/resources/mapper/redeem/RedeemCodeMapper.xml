<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.web.project.redeem.mapper.RedeemCodeMapper">

    <!-- 兑换码公共字段 -->
    <sql id="RedeemCodeColumns">
        id,
        batch_id,
        plan_id,
        code_hash,
        code_masked,
        status,
        expires_at,
        redeemed_user_id,
        redeemed_at,
        created_at
    </sql>

    <!-- 插入单个兑换码 -->
    <insert id="insert"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO redeem_code
        (
        batch_id,
        plan_id,
        code_hash,
        code_masked,
        status,
        expires_at
        )
        VALUES
        (
        #{batchId},
        #{planId},
        #{codeHash},
        #{codeMasked},
        #{status},
        #{expiresAt}
        )
    </insert>

    <!-- 批量插入兑换码 -->
    <insert id="batchInsert">
        INSERT INTO redeem_code
        (
        batch_id,
        plan_id,
        code_hash,
        code_masked,
        status,
        expires_at
        )
        VALUES
        <foreach collection="codes" item="code" separator=",">
            (
            #{code.batchId},
            #{code.planId},
            #{code.codeHash},
            #{code.codeMasked},
            #{code.status},
            #{code.expiresAt}
            )
        </foreach>
    </insert>

    <!-- 根据兑换码哈希查询 -->
    <select id="selectByCodeHash"
            resultType="com.web.project.redeem.entity.RedeemCode">
        SELECT
        <include refid="RedeemCodeColumns"/>
        FROM redeem_code
        WHERE code_hash = #{codeHash}
        LIMIT 1
    </select>

    <!--
        原子标记为已兑换。

        只有当前状态仍然是未兑换时才会更新成功。
        两个请求同时兑换同一个码时，只有一个请求能更新1行。
    -->
    <update id="markAsRedeemed">
        UPDATE redeem_code
        SET status = #{redeemedStatus},
        redeemed_user_id = #{userId},
        redeemed_at = #{redeemedAt}
        WHERE id = #{id}
        AND status = #{unusedStatus}
    </update>

    <!-- 兑换码列表公共查询条件 -->
    <sql id="RedeemCodeListWhere">
        WHERE code.batch_id = #{batchId}

        <if test="keyword != null and keyword != ''">
            AND (
            code.code_masked LIKE CONCAT('%', #{keyword}, '%')
            OR account.username LIKE CONCAT('%', #{keyword}, '%')
            OR account.nickname LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>

        <if test="status != null">
            AND code.status = #{status}
        </if>
    </sql>

    <!-- 查询批次下符合条件的兑换码总数 -->
    <select id="countByBatchIdAndCondition" resultType="long">
        SELECT COUNT(*)
        FROM redeem_code code
        LEFT JOIN user_account account
        ON account.id = code.redeemed_user_id

        <include refid="RedeemCodeListWhere"/>
    </select>

    <!-- 分页查询批次下的兑换码 -->
    <select id="selectPageByBatchIdAndCondition"
            resultType="com.web.project.redeem.mapper.model.RedeemCodeListRow">
        SELECT
        code.id,
        code.batch_id,
        code.plan_id,
        code.code_masked,
        code.status,
        code.expires_at,
        code.redeemed_user_id,
        account.username AS redeemed_username,
        account.nickname AS redeemed_nickname,
        code.redeemed_at,
        code.created_at
        FROM redeem_code code
        LEFT JOIN user_account account
        ON account.id = code.redeemed_user_id

        <include refid="RedeemCodeListWhere"/>

        ORDER BY code.id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

</mapper>